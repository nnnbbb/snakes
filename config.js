const config = {
  // player_speed: 8,
  // cloud_speed: 2,
  // enemy_speed: 5,
  // bullet_speed: 5,
  // fire_cooldown: 9,
  ground: {
    label: '砖块重置的坐标',
    value: 779,
    min: 500,
    max: 1000,
  }
}